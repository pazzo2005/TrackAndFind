spring.application.name=warehouse
spring.datasource.url=jdbc:postgresql://localhost:5432/warehouse
spring.datasource.username=postgres
spring.datasource.password=Aditya@2005
spring.datasource.driver-class-name=org.postgresql.Driver

# 1. FIXED DIALECT: Cleaned up the standard modern PostgreSQL dialect class string
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 2. SCHEMA STRATEGY: Leave as 'none' since you already created the table manually via SQL!
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# 3. EXTRA NETWORK PORT CONFIG (Highly Recommended for Mobile testing)
server.port=8080
server.address=0.0.0.0